// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("bitset", "for bitset support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("bitset", "enable bitset support", "no");

if (PHP_BITSET != "no") {
	EXTENSION("bitset", "bitset.c");
}

